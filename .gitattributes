# 换行符统一：仓库内一律 LF，Windows 工作区 checkout 为 CRLF
* text=auto eol=lf

# Windows 脚本必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Shell 脚本必须 LF（否则 Git Bash/WSL 执行报错）
*.sh text eol=lf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.ico binary
*.jar binary
*.db binary
